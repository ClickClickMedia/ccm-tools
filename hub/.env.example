# CCM API Hub - Environment Configuration
# Copy to .env and fill in your values
#
# IMPORTANT: Only database credentials and the encryption key live here.
# All other settings (API keys, OAuth, rate limits, etc.) are stored
# encrypted in the database and managed via the admin dashboard.

# Database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ccm_api_hub
DB_USER=ccm_hub
DB_PASS=your-database-password
DB_CHARSET=utf8mb4

# Encryption Key (64-char hex string — used to encrypt/decrypt all DB-stored secrets)
# Generate with: php -r "echo bin2hex(random_bytes(32));"
ENCRYPTION_KEY=your-64-char-hex-encryption-key

# Debug mode (set to true during development only)
APP_DEBUG=false

# Note: All other configuration (Google OAuth, Claude API key,
# PageSpeed API key, rate limits, etc.) is stored encrypted in
# the database and managed via the admin dashboard or setup wizard.
